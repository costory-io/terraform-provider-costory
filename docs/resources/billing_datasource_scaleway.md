---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "costory_billing_datasource_scaleway Resource - costory"
subcategory: "Billing Datasources"
description: |-
  Creates a Costory Scaleway billing datasource. See the full documentation here https://docs.costory.io/setup/billing#scaleway.
---

# costory_billing_datasource_scaleway (Resource)

Creates a Costory Scaleway billing datasource. See the full documentation [here](https://docs.costory.io/setup/billing#scaleway).

## Example Usage

```terraform
terraform {
  required_providers {
    costory = {
      source  = "costory-io/costory"
      version = ">= 0.1.0"
    }
  }
}

variable "costory_api_token" {
  type        = string
  description = "Costory API token."
  sensitive   = true
}

variable "scaleway_secret_key" {
  type        = string
  description = "Scaleway secret key used to fetch billing data."
  sensitive   = true
}

provider "costory" {
  token = var.costory_api_token
}

resource "costory_billing_datasource_scaleway" "main" {
  name            = "Scaleway Billing"
  secret_key      = var.scaleway_secret_key
  organization_id = "scaleway-organization-id"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Billing datasource display name.
- `organization_id` (String) Scaleway organization ID.
- `secret_key` (String, Sensitive) Scaleway secret key used to fetch billing data.

### Optional

- `access_key` (String, Sensitive) Optional Scaleway access key.
- `start_date` (String) Optional filter start date (ISO-8601).

### Read-Only

- `bq_table_uri` (String, Sensitive) BigQuery table URI created by Costory for billing data.
- `id` (String) Billing datasource ID returned by Costory.
- `status` (String) Datasource status returned by Costory.
- `type` (String) Datasource type. Always `Scaleway` for this resource.
